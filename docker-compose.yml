version: '3.9'

services:
  mariadb:
    container_name: snowshoe-db
    image: mariadb:10.8
    ports:
      - '3306:3306'
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: snowshoe
      MYSQL_USER: snowshoe
      MYSQL_PASSWORD: snowshoe
